<?xml version="1.0" encoding="UTF-8"?>
<project name="abraid-mp" default="build">
    
  <!-- Subfiles -->
  <import file="${basedir}/src/build.xml" />
  <import file="${basedir}/static-analysis.xml" />
  <import file="${basedir}/dependency-management.xml" />

  <!-- targets -->  
  <target name="clean" depends="clean.modules" description="cleanup everything"/>
  <target name="build" depends="acquire.libs, build.modules" description="build everything"/>

</project>
<div id="layerSelector">
    <h4 class="hidden" data-bind="css: { 'hidden': false }">You are viewing the
        <select data-bind="options: types, optionsText: 'display', value: selectedType"></select>
        of
        <select data-bind="options: diseases, optionsText: 'disease', value: selectedDisease"></select>
        for
        <select data-bind="options: runs, optionsText: 'date', value: selectedRun"></select>
    </h4>
</div>
